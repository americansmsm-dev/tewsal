# دليل النشر — Tewsal على Coolify

> ⚠️ **مهم:** النظام ده بيتعامل مع **فلوس حقيقية**. انشره على **staging (VPS B) الأول**،
> شغّله بجنبك أسبوعين وقارن التسويات بالورق، وبعدين حوّل التشغيل الحقيقي.
> ده مكتوب في الخطة (انضباط التحويل في مرحلة ٤).

---

## قبل ما تبدأ (محتاج منك)
- [ ] تجديد الـ VPS (VPS A بيخلص 2026-09-08 · VPS B بيخلص 2026-09-14).
- [ ] الكود على مستودع Git (GitHub) — Coolify بيسحب منه.
- [ ] دومين فرعي `app.tewsal.online` مشاور على السيرفر (A record).

## خطوات النشر

### ١) ارفع الكود على GitHub
```bash
cd "E:/moka saas project/tewsal"
git init
git add .
git commit -m "Tewsal system"
git branch -M main
git remote add origin git@github.com:<انت>/tewsal.git
git push -u origin main
```
(الأسرار مش هتترفع — `.gitignore` بيحميها.)

### ٢) في Coolify (على VPS)
1. **Postgres 16** كمورد جديد → فعّل النسخ الاحتياطي المجدول عليه.
2. **مورد جديد → من Git** → اختار الريبو والفرع `main`، نوع **Docker Compose**.
3. **Environment Variables**: انسخ من `.env.production.example` وحط القيم الحقيقية
   (`DATABASE_URL` من مورد Postgres، وولّد `SESSION_SECRET` و`ENCRYPTION_KEY` بـ
   `openssl rand -base64 32`).
4. **Pre-deploy command** (مهم جدًا — الترتيب ده):
   ```
   npm run db:migrate:prod
   ```
5. **Domain**: `app.tewsal.online` → Coolify بيطلّع SSL تلقائي.
6. **Deploy**.

### ٣) أول تشغيل — البذور وحساب المدير
مرة واحدة بس على قاعدة نظيفة (من محطة فيها كل الاعتماديات، أو Coolify terminal):
```bash
DATABASE_URL=<الإنتاج> npm run db:seed
```
هيطبع لك **اسم المدير وباسورد مؤقت** — غيّره من أول دخول.

### ٤) الفحص بعد النشر
- [ ] `https://app.tewsal.online/api/health` → `{"status":"ok","db":"up"}`
- [ ] دخول المدير → إنشاء شحنة تجريبية → طباعة بوليصة → افتح `/track/<awb>` من موبايل.
- [ ] شغّل `npm run invariants` على قاعدة الإنتاج — لازم يبقى نظيف.

---

## اللي لسه مش جاهز للتشغيل الحقيقي بفلوس (بصراحة)
| البند | الحالة |
|---|---|
| نواة الحسابات + الشحنات + التسويات + الخزينة | ✅ جاهزة ومتأكدة |
| بوابة التاجر + تطبيق المندوب (أونلاين) + الاستلام | ✅ جاهزة |
| **أوفلاين المندوب** | ⏳ لسه — المندوب محتاج نت |
| **الإشعارات (واتساب)** | ⏳ لسه |
| **بوليصة PDF بالجملة / الفاتورة الإلكترونية** | ⏳ لسه |
| **بروفة نسخ احتياطي واسترجاع + اختبار حمل** | ⏳ لسه |
| **أسبوعين تشغيل ظل** (مقارنة بالورق قبل التحويل الحقيقي) | ⏳ لازم |

**الخلاصة:** جاهز يتنشر على **staging** وتشتغل عليه وتجرّبه. **مش معتمَد للفلوس الحقيقية**
لحد ما تخلص الأوفلاين + النسخ الاحتياطي + أسبوعين الظل.
