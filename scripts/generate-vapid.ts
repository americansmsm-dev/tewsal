/**
 * ============================================================
 *  توليد مفاتيح الإشعارات (VAPID)
 * ------------------------------------------------------------
 *  الإشعارات على الفون واللاب محتاجة زوج مفاتيح:
 *    · العام  — المتصفح بياخده عشان يعمل الاشتراك (مش سر)
 *    · الخاص  — السيرفر بيوقّع بيه (**سر**، عمره ما يتحط في git)
 *
 *  ⚠️ شغّل الأمر ده **إنت** وحط الناتج في Coolify بنفسك —
 *     المفتاح الخاص لازم يفضل عندك إنت.
 *
 *    npm run vapid
 *
 *  ⚠️ لو غيّرت المفاتيح بعد ما ناس اشتركت، **كل الاشتراكات
 *     القديمة بتبطل** ولازم الناس تفعّل الإشعارات تاني. ولّدهم
 *     مرة واحدة وسيبهم.
 * ============================================================
 */
import webpush from "web-push";

const keys = webpush.generateVAPIDKeys();

console.log(`
${"═".repeat(64)}
  مفاتيح الإشعارات — توصّل
${"═".repeat(64)}

حط السطرين دول في متغيرات البيئة (Coolify → Environment Variables):

NEXT_PUBLIC_VAPID_PUBLIC_KEY=${keys.publicKey}
VAPID_PRIVATE_KEY=${keys.privateKey}
VAPID_SUBJECT=mailto:support@tewsal.online

${"─".repeat(64)}
  🔒 المفتاح الخاص (VAPID_PRIVATE_KEY) سر:
     · ماينزلش git ولا يتبعت في شات
     · لو اتسرّب، ولّد زوج جديد — بس ساعتها كل الاشتراكات
       الحالية بتبطل والناس هتحتاج تفعّل الإشعارات تاني
  ℹ️  المفتاح العام مش سر — المتصفح بياخده عادي.
${"═".repeat(64)}
`);
